from embed import download_model
# import sys
# import numpy as np

# assert len(sys.argv) >= 2, "Please provide an argument for the parameters required: \n \"
# assert --- "Please provide a valid argument for parameter "

download_model()
